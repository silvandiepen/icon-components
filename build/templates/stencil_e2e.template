import { newE2EPage } from '@stencil/core/testing';

describe('<%= filename %>', () => {
    it('renders', async () => {
    const page = await newE2EPage();
    await page.setContent('<%= filename %>');

    const element = await page.find('<%= filename %>');
    expect(element).toHaveClass('hydrated');
    });
});